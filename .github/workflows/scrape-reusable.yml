name: scrape-instagram-reusable

# Workflow reutilizável: qualquer repo cliente chama este workflow via workflow_call.
# A chave SCRAPER_API_KEY é compartilhada (uma conta scrape.do serve todos os clientes).
# O INSTA_USERNAME é configurado individualmente em cada repo cliente.

on:
  workflow_call:
    secrets:
      SCRAPER_API_KEY:
        required: true
      INSTA_USERNAME:
        required: true
      INSTA_SESSION_ID:
        required: true

jobs:
  scrape:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    steps:
      # 1. Checkout do repo CLIENTE (o repo que chamou este workflow)
      - name: Checkout client repo
        uses: actions/checkout@v4
        with:
          token: ${{ github.token }}

      # 2. Busca apenas o script do repo central (pepa667/luaink)
      #    Funciona mesmo que o cliente não tenha o script localmente.
      - name: Checkout scraper script
        uses: actions/checkout@v4
        with:
          repository: pepa667/luaink
          path: _scraper
          sparse-checkout: |
            insta/get-insta-feed.js
          sparse-checkout-cone-mode: false

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Prepare output directories
        run: mkdir -p www/images/insta

      - name: Run Instagram Scraper
        env:
          SCRAPER_API_KEY: ${{ secrets.SCRAPER_API_KEY }}
          INSTA_USERNAME: ${{ secrets.INSTA_USERNAME }}
          INSTA_SESSION_ID: ${{ secrets.INSTA_SESSION_ID }}
          IMAGES_DIR: ${{ github.workspace }}/www/images/insta
          LINKS_JSON_PATH: ${{ github.workspace }}/www/insta-links.json
          DEBUG_HTML_PATH: ${{ github.workspace }}/www/debug-insta.html
        run: node _scraper/insta/get-insta-feed.js

      - name: Upload debug HTML (disponivel mesmo em caso de falha)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: debug-insta-html
          path: www/debug-insta.html
          if-no-files-found: ignore
          retention-days: 3

      - name: Commit and Push Changes
        if: success()
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add www/images/insta/
          git add www/insta-links.json
          if git diff --cached --quiet; then
            echo "Nenhuma modificação pendente no feed estático."
          else
            git commit -m "chore: update instagram static feed images [skip ci]"
            git push
          fi
